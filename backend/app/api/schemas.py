"""Pydantic request/response models for the API layer.

See .agents/workflows/04-build-backend-api.md for the endpoint contracts these
schemas need to satisfy. Fill these in before wiring the routers.
"""

# TODO: RiskRequest / RiskResponse (state, sector -> risk_label, explanation, data_confidence)
# TODO: RightsRequest / RightsResponse (query, state?, language -> answer, citations, disclaimer)
# TODO: ResourcesResponse (state -> grievance channel contacts)
